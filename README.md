# LectureLens
LectureLens is a desktop app that extracts unique slides from YouTube or local videos, exporting them directly to text-searchable PDFs. It bypasses PowerPoint, using SSIM and pHash to detect slide changes. Includes a multi-video queue, OCR, and dark mode.
